; pnpm-specific settings for the workspace.
; When plain `npm` reads this file it will warn — that's fine, we install
; workspace deps via pnpm.
strict-peer-dependencies=false
auto-install-peers=true
shamefully-hoist=false
prefer-workspace-packages=true
